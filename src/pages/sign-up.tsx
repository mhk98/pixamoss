import React from "react";
import Layout from "@/components/layout/Layout";
import SignUpSec from "@/components/containers/SignUpSec";

const SignUp = () => {
  return (
    <Layout header={0} footer={0}>
      <SignUpSec />
    </Layout>
  );
};

export default SignUp;
