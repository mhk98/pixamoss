import React from "react";
import Layout from "@/components/layout/Layout";
import SignInSec from "@/components/containers/SignInSec";

const SignIn = () => {
  return (
    <Layout header={0} footer={0}>
      <SignInSec />
    </Layout>
  );
};

export default SignIn;
